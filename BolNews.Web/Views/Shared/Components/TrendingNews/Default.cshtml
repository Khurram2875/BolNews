@using BolNews.Web.Areas.Admin.ViewModels
@model List<PublicArticleVM>

@* <div class="container-fluid" style="border-top:solid; border-bottom:solid;border-width:thin; align-items:center;height: 40px;padding-top:05px">
    <h5 class="fw-bold mb-4 text-center">Trending</h5>
</div> *@

<!-- 🔥 Tabs -->
<div class="trending-tabs mb-3">

    <a href="#" class="trend-tab @(ViewBag.Type == "today" ? "active" : "")" data-type="today">
        Today
    </a>

    <a href="#" class="trend-tab @(ViewBag.Type == "week" ? "active" : "")" data-type="week">
        This Week
    </a>

    <a href="#" class="trend-tab @(ViewBag.Type == "month" ? "active" : "")" data-type="month">
        This Month
    </a>

</div>

<!-- 🔥 CONTENT CONTAINER -->
<div id="trending-container">
    @await Html.PartialAsync("~/Views/Shared/Components/TrendingNews/_TrendingList.cshtml", Model)
</div>