@* LatestNews/Default.cshtml — updated with bn- classes *@

@* <div class="bn-latest-header mb-3">Latest</div> *@

@* @foreach (var item in Model)
{
    <a href="/@item.CategorySlug/@item.Slug"
       class="bn-card-h track-click"
       data-article-id="@item.Id">
        <div class="bn-card-h-img">
            <img class="lazy-image"
                 src="/images/placeholder.png"
                 data-src="@item.FeaturedImageThumb"
                 alt="@item.Title"
                 loading="lazy" />
        </div>
        <div class="bn-card-h-body">
            <div class="bn-card-h-tag">@item.CategoryName</div>
            <div class="bn-card-h-title">@item.Title</div>
            <div class="bn-card-h-meta">@item.TimeAgo</div>
        </div>
    </a>
} *@
<div class="bn-latest-list">
    @foreach (var item in Model)
    {
        <a href="/@item.CategorySlug/@item.Slug"
           class="bn-latest-item track-click"
           data-article-id="@item.Id">
            <img src="@(item.FeaturedImageMedium ?? item.FeaturedImageLarge ?? "/images/placeholder.png")"
                 alt="@(item.FeaturedImageAltText ?? item.Title)"
                 loading="lazy" />
            <span>
                <strong>@item.Title</strong>
                <small>@item.TimeAgo</small>
            </span>
        </a>
    }
</div>