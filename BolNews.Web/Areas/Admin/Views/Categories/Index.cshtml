@using BolNews.Web.Areas.Admin.ViewModels
@model List<CategoryVM2>

@{
    ViewData["Title"] = "Categories";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2 class="mb-4">Categories</h2>

<a asp-action="Create" class="btn btn-primary mb-3">
    Create Category
</a>

<table class="table-striped table-hover table">
    <thead class="table-dark">
        <tr>
            <th>Name</th>
            <th>Slug</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            ViewBag.Level = 0;
            @await Html.PartialAsync("_CategoryRow", item)
        }
    </tbody>
</table>

@* @functions {
    void RenderTree(List<CategoryVM> categories, int level)
    {
        foreach (var item in categories)
        {
            <tr>
                <td>
                    @Html.Raw(new string('-', level * 3))
                    <strong>@item.Name</strong>
                </td>
                <td>@item.Slug</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning btn-sm me-1">Edit</a>

                    <form asp-action="Delete" asp-route-id="@item.Id" method="post" style="display:inline">
                        <button class="btn btn-danger btn-sm"
                                onclick="return confirm('Delete this category?');">
                            Delete
                        </button>
                    </form>
                </td>
            </tr>

            if (item.Children != null && item.Children.Any())
            {
                RenderTree(item.Children, level + 1);
            }
        }
    }
} *@